properties:
  message:
    properties:
      order:
        properties:
          status:
            type: string
            enum:
              - COMPLETE
              - ACTIVE
        required:
          - status
