properties:
  message:
    properties:
      order:
        properties:
          status:
            type: string
            enum:
              - CANCELLED
              - SOFT_CANCEL
        required:
          - status
