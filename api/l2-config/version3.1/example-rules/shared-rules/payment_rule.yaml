properties:
  message:
    properties:
      order:
        properties:
          payment:
            type: object
            properties:
              type:
                type: string
                enum:
                  - PRE-ORDER
                  - PRE-FULFILLMENT
                  - ON-FULFILLMENT
                  - POST-FULFILLMENT
              status:
                type: string
                enum:
                  - PAID
                  - NOT-PAID
              collected_by:
                type: string
                enum:
                  - DRIVER
                  - DRIVER_APP
                  - RIDER_APP
              params:
                type: object
                properties:
                  amount:
                    type: number
                  currency:
                    type: string
                required:
                  - amount
                  - currency
            required:
              - type
              - status
        required:
          - payment
