properties:
  message:
    properties:
      order:
        properties:
          payment:
            type: object
            properties:
              type:
                type: string
                enum:
                  - PRE-ORDER
                  - PRE-FULFILLMENT
                  - ON-FULFILLMENT
                  - POST-FULFILLMENT
            required:
              - type
        required:
          - payment
