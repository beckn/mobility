type: object
properties:
  context:
    allOf:
      - $ref: "../../../../schema/Context.yaml"
      - properties:
          action:
            enum:
              - feedback_form
        required:
          - action
  message:
    properties:
      feedback_form:
        $ref: ../../../../schema/Feedback.yaml
    required:
      - feedback_form
required:
  - context
  - message