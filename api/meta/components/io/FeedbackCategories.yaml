type: object
properties:
  context:
    allOf:
      - $ref: "../../../../schema/Context.yaml"
      - properties:
          action:
            enum:
              - feedback_categories
        required:
          - action
  message:
    type: object
    properties:
      feedback_categories:
        type: array
        items:
            $ref: '../../../../schema/Rating.yaml#/properties/rating_category'
    required:
      - feedback_categories
required:
  - context
  - message
