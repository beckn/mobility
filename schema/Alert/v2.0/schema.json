{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Alert",
  "type": "object",
  "description": "A notification of a service disruption, delay, or advisory affecting one or more routes, stops, or vehicle journeys.",
  "properties": {
    "cause": {
      "description": "Cause of the alert (e.g. TECHNICAL_PROBLEM, STRIKE, ACCIDENT)",
      "type": "string"
    },
    "effect": {
      "description": "Effect on service (e.g. DETOUR, REDUCED_SERVICE, NO_SERVICE)",
      "type": "string"
    },
    "url": {
      "description": "URL to a page with more information about the alert",
      "type": "string",
      "format": "uri"
    },
    "headerText": {
      "description": "Short header text for the alert",
      "type": "string"
    },
    "descriptionText": {
      "description": "Detailed description of the alert",
      "type": "string"
    },
    "activePeriod": {
      "description": "Time period during which the alert is active",
      "$ref": "https://schema.beckn.io/TimePeriod/v2.1/schema.json"
    },
    "informedEntity": {
      "description": "Transport entities affected by this alert",
      "$ref": "https://schema.beckn.io/EntitySelector/v2.0/schema.json"
    },
    "id": {
      "description": "Unique identifier for the alert",
      "type": "string"
    },
    "descriptor": {
      "description": "Human-readable description of the alert",
      "$ref": "https://schema.beckn.io/Descriptor/v2.1/schema.json"
    },
    "validity": {
      "description": "Time period during which the alert is active",
      "$ref": "https://schema.beckn.io/TimePeriod/v2.1/schema.json"
    },
    "status": {
      "description": "Current status of the alert",
      "$ref": "https://schema.beckn.io/State/v2.0/schema.json"
    }
  },
  "additionalProperties": false
}
