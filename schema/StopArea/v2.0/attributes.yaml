openapi: 3.1.0
info:
  title: StopArea
  version: 2.0.0
  description: Schema definition for the StopArea entity in the Beckn Mobility domain.
  license:
    name: CC-BY-NC-SA 4.0 International
    url: https://creativecommons.org/licenses/by-nc-sa/4.0/deed.en
  contact:
    name: Beckn Labs
    url: https://beckn.io
components:
  schemas:
    StopArea:
      type: object
      title: StopArea
      description: A group of stops that collectively define a zone from which a demand-responsive
        service may pick up or drop off passengers.
      x-tags:
        - mobility
        - geosparql
        - ride-hailing
      properties:
        areaId:
          description: Unique identifier for the stop area
          type: string
        areaName:
          description: Human-readable name of the stop area
          type: string
        stops:
          description: Stops contained within this area
          $ref: https://schema.beckn.io/Stop/v2.0/attributes.yaml#/components/schemas/Stop
        geometry:
          description: GeoJSON geometry defining the area boundary
          $ref: https://schema.beckn.io/GeoJSONGeometry/v2.0/attributes.yaml#/components/schemas/GeoJSONGeometry
        id:
          description: Unique identifier for the location
          type: string
        descriptor:
          description: Human-readable description of the location
          $ref: https://schema.beckn.io/core/v2.0/Descriptor/attributes.yaml#components/schemas/Descriptor
        gps:
          description: GPS coordinates as a latitude,longitude string
          type: string
        address:
          description: Physical address of the location
          $ref: https://schema.beckn.io/Address/v2.0/attributes.yaml#/components/schemas/Address
        city:
          description: City name
          type: string
        country:
          description: ISO 3166-1 alpha-2 country code
          type: string
        geojson:
          description: GeoJSON geometry object representing this location
          $ref: https://schema.beckn.io/GeoJSONGeometry/v2.0/attributes.yaml#/components/schemas/GeoJSONGeometry
      additionalProperties: false
